# Notebooks

Thin drivers only. Training, metrics, and tracking live in `src/` and `scripts/`.

| Notebook | Role |
|----------|------|
| [01_train_driver.ipynb](01_train_driver.ipynb) | Calls `scripts/train.py` smoke flags and points at `mlflow ui` |

Do not paste TFT training loops into notebooks.
