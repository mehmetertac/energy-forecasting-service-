# Dashboard

**Placeholder** for the Week 9 Streamlit demo.

## Compose stub (now)

`docker compose up` starts a minimal stdlib HTTP server on **:8501** ([`stub_server.py`](stub_server.py)) that returns plain text: “not built yet.” No Streamlit dependency — keeps the stack lean.

## Later

Live (or replayed) TFT forecasts with **P10–P90 uncertainty bands** and a short dispatch reading of the band, reading from `POST /forecast` on the API (`http://api:8000` inside compose).
